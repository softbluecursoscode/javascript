/**
 * EXERCÍCIO
 * ---------
 * 
 * Crie um módulo com uma função utilitária que transforma a primeira letra de uma string em letra maiúscula.
 * Depois, crie um arquivo que importa e usa este recurso.
 */

import { StringUtils } from "./string_lib.js";

console.log(StringUtils.capitalize("abcd"));